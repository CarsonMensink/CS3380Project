1. Create venv
2. Activate venv
3. pip install -r requirements.txt
4. Run everything in create_table to drop all tables if exist and recreate them
5. Run everything in populate_tables to create synthetic date
6. In termainl run 'streamlit run app.py'
