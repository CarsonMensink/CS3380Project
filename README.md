1. Activate venv
2. pip install -r requirements.txt
3. Run everything in create_table to drop all tables if exist and recreate them
4. Run everything in populate_tables to create synthetic date
5. In termainl run 'streamlit run app.py'
